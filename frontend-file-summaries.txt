{
  "frontend/src/App.tsx": "Main app orchestrator updated to store `pendingMonsters: GeneratedMonster[]` in state, thread them through `handleStartDungeon`, and pass them as the `monsters` prop to `DungeonRunScreen`; clears pending monsters on dungeon complete, death, and back-to-character-select.",
  "frontend/src/components/CharacterSheet.tsx": "Character sheet displaying stats, abilities, and equipment; uses the top-level `ABILITIES` ES module import instead of a `require()` call when rendering the abilities list; all other logic unchanged.",
  "frontend/src/components/DungeonSelectScreen.tsx": "Dungeon select screen that now calls `generateMonster()` for each monster slot when starting a dungeon run, passing the generated `GeneratedMonster[]` array to `onStartDungeon`; the `onStartDungeon` callback signature is updated to accept an optional monsters array.",
  "frontend/src/components/DungeonRunScreen.tsx": "Dungeon run screen updated to accept an optional `monsters?: GeneratedMonster[]` prop and pass it to `simulateCombat`; displays the first monster's procedural name in the header subtitle alongside the character info.",
  "frontend/src/components/InventoryScreen.tsx": "Inventory management screen importing `LocalCharacter` from `types/game`; all other logic (equip/unequip/stash) unchanged.",
  "frontend/src/components/MarketplaceScreen.tsx": "Marketplace screen importing `LocalCharacter` from `types/game` and accepting `character: LocalCharacter | null` to handle cases where no character is selected.",
  "frontend/src/components/LeaderboardScreen.tsx": "Leaderboard screen with Softcore/Hardcore realm tabs showing mock ranked adventurers; `currentUsername` prop is now optional so it can be rendered without a profile loaded.",
  "frontend/src/components/AbilitySelectModal.tsx": "Ability selection modal that imports `LocalCharacter` from `types/game`, uses an `onConfirm(abilities: string[])` callback instead of separate purchase/equip/unequip props, and allows toggling abilities up to the character's total ability point limit.",
  "frontend/src/components/LevelUpModal.tsx": "Stat allocation modal that uses `calculateUnspentStatPoints` as the single source of truth for available points; allows allocating all unspent points and calls `onConfirm` with the new base stats and updated total spent count.",
  "frontend/src/components/ProfessionsPlaceholder.tsx": "Coming Soon placeholder screen for the Professions feature showing four planned profession types (Smithing, Alchemy, Tinkering, Gathering) with descriptions.",
  "frontend/src/components/ShrinesPlaceholder.tsx": "Coming Soon placeholder screen for the Shrines feature showing four planned shrine types with animated glow effects and flavor descriptions.",
  "frontend/src/components/ItemTooltip.tsx": "Item tooltip and card components updated to prominently display \"Base Damage\" for Weapon items and \"Base Defense\" for Armor items before the affix list, using orange and cyan accent colors respectively.",
  "frontend/src/data/monsters.ts": "Defines monster data with 12 prefixes and 12 suffixes (each with HP/attack/defense stat modifiers), 10 base monster templates, and a `generateMonster()` function that combines a random prefix + base template + suffix into a `GeneratedMonster` with fully resolved combined stats and a \"Prefix BaseName Suffix\" name.",
  "frontend/src/engine/lootGenerator.ts": "Loot generator with all original exports restored (`generateLoot`, `ItemAffix`, `AffixStat`, `icon` field on `GeneratedItem`), plus a new `generateStarterEquipment()` function that returns a Worn Sword (5 base damage) and Worn Chestplate (5 base defense) at item level 1 with Common rarity.",
  "frontend/src/engine/combatEngine.ts": "Combat simulation engine updated with an additive XP formula (`baseXP(level) + STAT_BONUS_PER_POINT * (scaledHp + scaledAtk + scaledDef)`) where all three stats contribute equally; accepts optional `preGeneratedMonsters` so procedurally generated monsters flow directly into combat; combat log now uses \"You are battling\" phrasing for monster encounters.",
  "frontend/src/hooks/useQueries.ts": "React Query hooks for all backend operations; `useSetCharacterHp` no longer invalidates the `characters` query on success to prevent background re-fetches from overwriting live local HP state during regen ticks.",
  "frontend/src/components/CharacterCreation.tsx": "Character creation form that accepts `existingCharacters: Character[]` (backend type), converts stat values to `BigInt` for the backend call, and supports both `onBack` and `onCancel` props.",
  "frontend/src/data/abilities.ts": "Ability definitions with explicit damageMultiplier and damageType fields required by the combat engine's ability cycling system; 10 abilities across Warrior/Mage/Rogue/Universal archetypes.",
  "frontend/src/types/game.ts": "Core game types including `LocalCharacter`, `BaseStats`, `CharacterStats`, and utility functions `calculateMaxHp`, `calculateAvailableAbilityPoints`, `calculateUnspentStatPoints`, `xpForLevel`/`getXpForLevel` (alias), and `calculateLevel`; imports `GeneratedItem` from lootGenerator instead of using dynamic import.",
  "frontend/src/hooks/useLocalCharacter.ts": "Local character state hook that now tracks which character ID was last initialized via a ref; on backend re-fetches of the same character, it preserves the live local HP instead of resetting from the backend value, while still using backend HP as the authoritative source on first load.",
  "frontend/src/hooks/useHealthRegen.ts": "Health regen hook that fires +2 HP/sec when out of combat and below maxHp, using refs to avoid stale closures; regen starts from the current persisted HP value and syncs to the backend periodically.",
  "frontend/src/components/HealthBar.tsx": "Reusable health bar component displaying current/max HP as a filled bar with color transitions (green/yellow/red) and numeric label; supports compact mode for mobile.",
  "frontend/src/components/CharacterSelectScreen.tsx": "Character selection screen with added `isError` and `onRetry` props; renders a clear error message with an `AlertTriangle` icon and a Retry button when the character fetch fails, replacing the previous blank/spinner state.",
  "frontend/src/components/Navigation.tsx": "Sticky navigation header that now accepts an `isSavingHp` prop; shows a spinner and \"Saving...\" label on the back button while HP is being persisted to the backend, and disables the button during the save to prevent double-navigation.",
  "frontend/src/index.css": "Global styles with OKLCH dark theme, health bar color tokens (health-high/mid/low), rarity color tokens, and Cinzel display font import.",
  "frontend/tailwind.config.js": "Tailwind configuration mapping all OKLCH CSS tokens to theme colors including surface-1/2, health-high/mid/low, rarity tiers, and Cinzel/Inter font families.",
  "frontend/src/components/XPBar.tsx": "XP progress bar component using `xpForLevel` (correct export name from `types/game`) instead of the old `getXpForLevel`."
}